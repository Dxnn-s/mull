{
  "name": "mull",
  "private": true,
  "version": "0.0.0",
  "description": "Opal for AI. Catches lazy prompts to ChatGPT / Claude / Gemini and makes you pass a short quiz on the concept before the answer.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev:web": "pnpm --filter @mull/web dev",
    "build:ext": "pnpm --filter @mull/extension build",
    "eval": "pnpm --filter @mull/core eval"
  }
}
