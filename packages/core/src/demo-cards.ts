/**
 * Kept so existing imports keep working. The bank moved to cards.ts when it
 * stopped being a demo fixture and became how the app answers by default.
 */
export { demoCard, DEMO_CONCEPTS, CARDS, BANK_SUBJECTS, pickCard } from './cards.ts';
export type { BankCard } from './cards.ts';
